#include <vector>
#include "order.h"

class ValuationEngine {
    public:
    
    double priceAsset(std::vector<Order> &bids, std::vector<Order> &asks) const { 
        double valuation;

        double bidVolume = 0;
        double askVolume = 0;
        
        return valuation;
    }
};